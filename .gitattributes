# 跨平台换行符规范化（公开仓库协作推荐）
# 入库统一 LF，检出时按各平台规则转换，避免 Windows/Linux/macOS 协作者产生 CRLF 冲突
* text=auto eol=lf

# 明确二进制文件，避免被当作文本处理导致损坏
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.jks binary
*.keystore binary
*.p12 binary
*.apk binary
*.aab binary
